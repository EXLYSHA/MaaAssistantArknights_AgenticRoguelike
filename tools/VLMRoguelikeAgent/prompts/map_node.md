本次决策：在当前楼层的可达节点中选下一个去的节点。

**截图 UI 区域（请按以下位置精确读取数值）：**
- **左上角**：目标生命值（如 `8/8`，斜杠左为当前剩余生命值，右为当前生命值上限。） → 等级（如 `0/24`，斜杠左为当前经验值，右为到达下一等级需要的经验值。需要通过战斗节点升级，升级可提升编队最大人数，生命值上限与希望）。
- **右上角**：从左到右是 **希望**（金色，第一个箭头显示的金色数字为当前希望，第二个箭头显示的绿色数字为希望上限。）和 **源石锭**（绿色三角形图标，右侧是数量）。
- **左下角**：**收藏品**（藏品列表入口，本步通常无需读数）。
- **右下角**：从左到右是 **思维负荷**（如 `1/14`，斜杠左为当前，右为上限）、**构想 / 思绪**（多边形小图标下方的数字）、**干员**与**编队**入口。

**关键资源用途：**
- **构想 / 思绪**（右下）：是开**纵向通路 / 跨列连接 / 部分"不期而遇"等节点**的代价（一般每条 2 构想）；某些场景还能刷新商店或关卡。**如果构想数 < 节点需要的代价，绝对不要选这个节点**——点了会被拦下，主流程会卡住。
- **源石锭**（右上）：用于商店购物。源石锭少时绕路去商店收益不大。
- **HP / 生命值**（左上）：HP 低时优先 safehouse / 商店 / 避战节点。
- **思维负荷**（右下）：满了会有惩罚，影响阵容部署。
- **希望**（右上）：影响招募。

**判定"需消耗构想的节点"是否能走：**
1. 在截图上读取右下角构想数；
2. 仅纵向同一列的节点移动会消耗构想，若该节点的代价（一般写在节点旁的小图标上）> 当前构想数，**严禁选**；
3. 改选不消耗构想的常规节点。

**通用策略：**
- HP ≤ 3：优先 safehouse / shop / encounter（避战补血）
- 队伍未成型：优先 shop（藏品）/ elite（精英战奖励多）
- 已成型：可冲 elite / boss
- 不期而遇期望收益正但有风险；优先选不消耗构想的
- 商店多走一次比少走一次好（藏品复利），但要看源石锭

把多步意图写到 `planned_path_next`。`node_id` 必须取自当前可走列表（reachable_node_ids）。
