本次决策：选择开局分队。

不同分队的初始干员、初始藏品、初始希望、招募名单都不同，会显著影响整局开局节奏。
请综合当前的整体目标（稳定通关 / 快速通关 / 路线规划 / 商店滚资源等）和分队特性挑选。

如果请求里是 visual_slots 模式，必须优先看截图本身：从截图序列中选择一个没有锁图标、没有“解锁条件”的完整分队卡片，返回它的 screenshot_index 和 slot_index。OCR 列表只作为辅助信息，可能包含说明文字、缺字、错字或把未解锁分队误判为可选，不能只凭 OCR 名称做决定。能看清名称时附带 squad_name；看不清名称时仍然返回视觉槽位。

如果请求不是 visual_slots 模式，必须只从“可选分队”列表中逐字返回一个名称。截图里带锁或显示“解锁条件”的分队即使战略价值更高也禁止选择。常见有力分队：蓝图测绘分队、博闻广记分队、魂灵护送分队、指挥分队、后勤分队等。
